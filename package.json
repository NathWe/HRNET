{
  "name": "p12",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "build:hrnet": "turbo build --filter hrnet",
    "build:types": "turbo build:types",
    "dev": "turbo dev",
    "dev:hrnet": "turbo dev --filter hrnet",
    "dev:datatable": "turbo dev --filter data-table",
    "preview": "turbo preview",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.0.12",
    "typescript": "^5.4.5"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  }
}
